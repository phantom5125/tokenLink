# TokenLink v0.2.2 Integration Validation — 2026-08-30

This report covers the local integration candidate built from `origin/main`
commit `acaea3a` and the rebased Bluetooth diagnostics, session focus, and
Sessions status branches. Automated evidence and physical-device evidence are
kept separate; a compiled or packaged result is not treated as a C152 display,
radio, or macOS interaction observation.

## Candidate identity

- Integration branch: `codex/release-v0.2.2`
- Bluetooth diagnostics: `cc47518`
- Session focus: `d83958b`
- Sessions status and pagination: `b99a818`
- Release metadata baseline: `a095d65`
- Session-focus delivery hotfix and packaged binary source: `d3b1cbc`
- Session execution/attention-state fix and packaged binary source: `875e5ef`
- Long-running rollout-state fix and packaged binary source: `3abd584`
- C152 short-click wake fix and packaged firmware source: `0c568fb`
- Mac identity: `app.tokenlink`, version `0.2.2`, build `7`
- C152 identity: `0.2.2-tokenlink`, protocol v1/v2

## Results

| Layer | Evidence | Status | Limitations |
| --- | --- | --- | --- |
| Rebase and integration | Each feature branch is based directly on `origin/main` `acaea3a`, then was merged into a fresh `codex/release-v0.2.2` branch. Rewritten feature tips and the replaced release placeholder were retained under dated `backup/` refs. | PASS | The integration branch remains local and has not run hosted CI or public review. |
| Version and documentation alignment | `Info.plist`, C152 firmware identity, simulator identity, CHANGELOG, English and Simplified Chinese READMEs, firmware README/NOTICE, release notes, and download names agree on 0.2.2. The unchanged protocol contract intentionally keeps 0.2.1 as the minimum host version. | PASS | The README download links become live only after publishing the matching GitHub tag and assets. |
| Mac automated suite | `bash scripts/test.sh` completed all 190 Swift tests, including compact command decoding, cancellation-safe command-stream replacement, Bluetooth diagnostic projection/redaction, explicit focus routing, ten-second lifecycle polling, backward chunk scanning beyond 256 KB, conservative terminal mapping, attention acknowledgement/reset, complete pagination, stable slot priority, and aggregate active count. | PASS | Injected transports do not prove visible Codex task selection or status rendering on the physical watch. |
| Source quality and privacy | Strict Swift format lint, `git diff --check`, and `bash scripts/privacy_scan.sh` passed. | PASS | Automated scanning complements review; it cannot recognize every future sensitive-data pattern. |
| Codex App Server pagination contract | The implementation requests `thread/list` in `updated_at` order, follows every `nextCursor`, and rejects repeated cursors or the configured safety cap. This matches the [official paginated-list contract](https://learn.chatgpt.com/docs/app-server#list-threads-with-pagination--filters). | PASS | The live local Codex dataset should still be checked with more than three active tasks. |
| Mac/firmware BLE contract | `python3 scripts/check_firmware_contract.py` confirmed that Mac and C152 agree on C01-C04 and protocol v1/v2. | PASS | A source contract check does not establish that a physical C04 notification subscription succeeded. |
| C152 build and native tests | PlatformIO Core 6.1.19 with Espressif32 6.12.0 built the real ESP32-S3 release image. All twelve host-native firmware test executables passed, including short clicks between direct PM1 samples, compact focus, `seen`, explicit-empty replacement, unknown-state, and presentation checks. RAM was 56,128 / 327,680 bytes (17.1%); flash was 1,330,917 / 6,553,600 bytes (20.3%). | PASS | Compilation and native tests do not exercise the AMOLED, touch controller, Bluetooth radio, side button, or PM1 power hardware. |
| Firmware packager | Both packager unit tests passed. The packager generated the combined image, split-input archive, manifest, and checksums from source revision `0c568fb`; every checksum verified. | PASS | Artifact integrity is recorded separately from physical behavior. |
| Mac release artifact | `TokenLink-0.2.2-macos-arm64.zip` extracted successfully; the contained bundle is arm64, `app.tokenlink` 0.2.2 (7), and passed strict deep signature verification. SHA-256 is `8ce162ceb0a45ff67b32fb9edcc20bbb9bec3e1150c4c8684c4ee8064b659ce9`. | PASS | The local artifact is ad-hoc signed with no Team ID and is not notarized. |
| Firmware release artifacts | `TokenLink-StopWatch-C152-0.2.2.bin`, `TokenLink-StopWatch-C152-0.2.2.zip`, and `firmware-manifest.json` match `SHA256SUMS`. The merged image is 1,396,816 bytes, has SHA-256 `95a67bf63b5ba4b1966ba0d98994822336f8f1490c5239c2798c90b8fb041b88`, and flashes at offset `0x0`. | PASS | The development artifact is local until the release is published. |
| Acceptance process isolation | The installed candidate is the only running TokenLink process and resolves to `/Applications/TokenLink.app`, version 0.2.2 (7). | PASS | Worktree and legacy bundles must remain closed during acceptance because duplicate bundle identities can contend for BLE state. |
| Exact-candidate physical C152 flash and boot | Esptool flashed the merged `0c568fb` image (SHA-256 `95a67bf6…1b88`) at `0x0` on the confirmed Espressif `303A:1001` device with serial suffix `0B:E4`. It verified all 1,396,816 written bytes, and a fresh reset probe observed the PM1 configuration plus `CODEX_MICRO_STOPWATCH_READY`. | PASS | Serial boot evidence does not substitute for the user's visible short-click sleep/wake check. |
| Physical red-button wake | The runtime now merges M5Unified's latched click IRQ with 25 ms direct state sampling and deduplicates tracked presses. The exact image is installed and the missed-click path has a host-native regression test. | PENDING | Perform one visible single-click sleep followed by one visible single-click wake; the acceptance trace should show `POWER desk_sleep`, then `POWER red irq_click wake=1` and `POWER desk_wake` when the short click uses the IRQ path. |
| Physical Bluetooth, UI, focus, status acknowledgement, and reconnect | Build 6 proved post-flash protocol v2/C04 and three-provider sync, then exposed that a long-running rollout could move `task_started` beyond the 256 KB tail. Build 7 contains the backward chunk fix and is installed, but its new ad-hoc binary is waiting for the required Keychain approval before live Active stability can be rechecked. | PENDING | Approve build 7 once, then verify Active remains running and retain the real `ACTION` → `OPENED` visual check for a task that exposes a needs-input flag. |
| Public distribution | Run hosted CI/review and build a Developer ID-signed, notarized stable Mac archive. | PENDING | The verified local ad-hoc artifact is suitable for acceptance testing, not a frictionless stable public download. |

## Physical acceptance checklist

Use the Mac artifact generated from `3abd584` and the firmware artifact generated
from `0c568fb`. Record any
failure with the visible status text and the exported redacted diagnostics;
do not include peripheral UUIDs, task identifiers, tokens, or provider payloads.

1. Quit every running TokenLink instance before installing the candidate. Launch
   only the verified 0.2.2 app from `/Applications/TokenLink.app`, confirm there
   is exactly one TokenLink process, and confirm its executable resolves inside
   that bundle. Do not launch a worktree-built app or the legacy
   `io.github.phantom5125.tokenlink` bundle during acceptance.
2. Confirm TokenLink shows version 0.2.2 (7), the C152 System page shows
   `0.2.2-tokenlink`, and protocol v2 is active.
3. Flash `TokenLink-StopWatch-C152-0.2.2.bin` at offset `0x0` to the explicitly
   confirmed C152 port. Verify every esptool hash and observe a fresh
   `CODEX_MICRO_STOPWATCH_READY` marker after reset.
4. Launch TokenLink and open StopWatch without pressing Scan or Sync. Confirm
   that merely opening the page does not initialize CoreBluetooth or display a
   Bluetooth permission request.
5. Press Scan or Sync and complete the explicit authorization flow. Confirm the
   diagnostics distinguish permission, adapter, binding, connection progress,
   C04 notifications, last sync, and redacted failure state. Exercise the denied
   or disabled-adapter path once and verify the recovery guidance.
6. Export diagnostics and inspect the text before sharing. Confirm it contains
   no peripheral UUID, task ID, token, credential, or provider payload.
7. With more than three active Codex tasks, verify the watch shows the exact
   `N ACTIVE / 3 SHOWN` aggregate. Needs-input and failed tasks should outrank
   running tasks, and repeated refreshes should keep unchanged task slots stable.
8. Verify each row remains distinguishable by shape/text as well as color: blue
   `RUNNING` orbit, unseen amber `ACTION` pulse, opened amber `OPENED` ring,
   explicit green `DONE` check, red `FAILED` alert, and grey `UNKNOWN` dot.
   Running and ambiguous rows must never show a green check. Check normal and
   dim brightness for clipping or excessive flicker.
9. Use the Mac-side focus control on every displayed row and confirm the exact
   Codex task becomes visible. Repeat from the matching C152 row through C04.
   Confirm TokenLink reports delivered, fallback, expired slot, unsupported
   provider, or Codex unavailable accurately without logging the task ID. A
   pending row must change from `ACTION` to `OPENED` without becoming `DONE`;
   a newer pending event must restore `ACTION`.
10. Quit Codex and retry focus, then restart Codex and retry. Confirm the visible
   outcome changes from unavailable/fallback to delivered and the intended task
   is selected.
11. Relaunch TokenLink, power-cycle the C152, and sleep/wake the Mac. Confirm the
    current binding reconnects, C04 resumes, the diagnostics advance, and no
    unexplained repeated authorization request appears.
    Separately, single-click the red button to enter desk sleep, then
    single-click it again and confirm the display wakes without a second press.
12. Confirm TokenLink startup does not access `Claude Code-credentials`. Enable
    Claude explicitly and verify any Keychain prompt appears only at that point,
    explains the authorization scope, and identifies the application as
    TokenLink rather than a legacy bundle name.
13. Run a 15-30 minute interaction soak covering refresh, page changes, focus,
    reconnect, and dim/wake. Track the separate 24-hour power/battery soak as
    follow-up evidence if it remains outside the 0.2.2 release gate.

## Release decision

The source, Mac archive, exact C152 artifact/flash, and boot marker are ready for
user acceptance. Do not tag or publish the stable release until the visible
single-click wake check passes, build 7 receives its local Keychain approval,
long-running Active stability and a real needs-input `ACTION` → `OPENED` check
pass, the remaining checklist passes, and the pushed commit has passed hosted
CI and review. The current evidence does not claim notarization, Intel support,
a Costs page, quota-SDK integration, or installer work for 0.2.2.
