# TokenLink v0.2 Release Readiness — 2026-08-30

This report separates source-merge readiness from public-release readiness. A
passing build is not treated as proof of notarization, a physical display pass,
or long-duration power behavior.

## Results

| Layer | Evidence | Status | Limitations |
| --- | --- | --- | --- |
| Mac automated tests | `bash scripts/test.sh` completed 170 tests, including full active-count projection, Codex task deep-link handling, Bluetooth v1/v2 negotiation, provider-logo loading, and rollout-path containment. | PASS | Network/provider tests use fixtures or injected transports unless separately identified. |
| Swift format | Strict `swift format lint` passed for `Package.swift`, `Sources`, and `Tests`. | PASS | None. |
| Privacy scan | `bash scripts/privacy_scan.sh` passed. | PASS | Complements review; it cannot recognize every future secret format. |
| Release app build | `bash scripts/build_release_artifact.sh` built `TokenLink.app`, copied the SwiftPM logo bundle into `Contents/Resources`, signed the complete app, archived it, extracted it, and verified its executable, metadata, resources, and signature. | PASS | The local artifact is Apple Silicon and ad-hoc signed. |
| Release checksum | `TokenLink-0.2.0-macos-arm64.zip.sha256` passed `shasum -a 256 -c` beside the archive. | PASS | Integrity only; it is not code provenance or notarization. |
| RC macOS distribution | The arm64 app archive and checksum were rebuilt and reverified for `v0.2.0-rc.1`. | PASS | The RC app is an explicitly labeled ad-hoc development artifact, not a notarized default download. |
| Public macOS distribution | `security find-identity -v -p codesigning` found no Developer ID identity, `notarytool` found no TokenLink profile, and the repository has no release-signing secrets. | NOT VERIFIED | Publish Developer ID-signed and Apple-notarized assets before promoting a stable default download. |
| Intel distribution | Source support remains declared and CI builds the current runner architecture. | NOT VERIFIED | Build and test an `x86_64` artifact or produce and test a universal binary before claiming a binary download supports Intel. |
| C152 firmware build | `pio run -e m5stack-stopwatch` completed with 56,120 bytes RAM and 1,329,377 bytes flash used. | PASS | Compilation is not a flash or display observation. |
| Firmware model/protocol tests | Nine native test executables passed, including protocol v2, aggregate active count, watch model, and raise-to-wake. | PASS | Host-native logic does not cover real touch, BLE radio, or PM1 behavior. |
| Four-page native preview | `pio run -e native-preview` passed for the current Home, Quota, Sessions, and System renderer. | PASS | The native preview is not the circular AMOLED panel. |
| Exact release-candidate C152 flash | After explicit confirmation of `/dev/cu.usbmodem101` (Espressif `303A:1001`, serial ending `0B:E4`), PlatformIO flashed firmware commit `0d5bf6c`; esptool verified every written image hash and reset the ESP32-S3. | PASS | This records the confirmed device used for the test without publishing its complete identifier. |
| Firmware boot and GATT | A reset serial probe observed `CODEX_MICRO_STOPWATCH_READY`, protocol-v2 identity, vendor HID, and the quota write/capabilities/command characteristics. | PASS | Serial startup evidence does not prove every screen pixel or gesture target. |
| Live v2 Mac-to-watch sync | The installed `/Applications/TokenLink.app` 0.2.0 (build 2) reported two active Codex sessions, negotiated protocol v2, and automatically synced Codex, Kimi, and MiniMax. The watch serial log received Codex (one window, three work items), Kimi (two windows), and MiniMax (two windows). | PASS | GLM and Claude were not selected in this live payload. |
| Physical display and touch smoke test | Inspect all four circular pages and exercise Home-to-Sessions, Codex focus, refresh, side-button, and swipe behavior on the panel. | NOT VERIFIED | The automated environment cannot see or touch the physical AMOLED; do not infer this result from serial/BLE success. |
| Power behavior | Run the planned 24-hour desk-sleep, raise/tap wake, charging, and reconnect soak. | NOT VERIFIED | Build or short interaction cannot establish battery or wake reliability. |
| Companion firmware distribution | The verified source was pushed to `phantom5125/codex-micro-stopwatch` at `0d5bf6c`, upstream PR #5 was opened, and a C152-only merged RC image plus checksum were generated for the TokenLink prerelease. | NOT VERIFIED | The upstream PR is not merged/tagged; the combined image is derived from the exact flashed components but was not separately reflashed after merging. |

## Merge decision

The TokenLink source is ready for normal code review and merge: automated tests,
formatting, privacy checks, firmware build/tests, and the relocatable Mac archive
all pass. The Quick Start now gives honest source paths for users with and
without a C152.

The source is ready to merge and the evidence supports an explicitly labeled
`v0.2.0-rc.1` prerelease. It does not support promoting a stable, zero-tool
public download yet. Before stable `v0.2.0`:

1. Complete the physical four-page/touch smoke test and the planned power soak.
2. Merge/tag the matching `codex-micro-stopwatch` source and publish its own
   documented firmware artifact.
3. Build TokenLink with a Developer ID identity, notarize it, and publish the app
   archive plus SHA-256 checksum in a GitHub Release.
4. Either add an Intel artifact/universal build or label the first binary release
   Apple-Silicon-only.
5. Observe the hosted CI result after pushing; a local reproduction does not
   substitute for the remote workflow.
