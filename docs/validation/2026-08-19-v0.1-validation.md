# TokenLink v0.1 验证报告

日期：2026-08-24（验证执行日；计划制定于 2026-08-19）

| Layer | Command/Action | Evidence | Status | Limitations |
| --- | --- | --- | --- | --- |
| 编译（clean build） | `swift package clean && swift build` | Build complete，无警告阻断 | PASS | — |
| 单元测试 | `.local/run_tests.sh`（swift build --build-tests + 手工 Swift Testing 运行器） | 43/43 通过 | PASS | 本机无 Xcode，`swift test` 官方运行器不可用（缺 xctest 工具）；用等价手工运行器执行，CI（macos-15 有 Xcode）使用 `swift test` |
| Lint | `swift format lint --recursive --strict Sources Tests` | 无违规 | PASS | — |
| 隐私扫描 | `bash scripts/privacy_scan.sh` | OK | PASS | — |
| 打包 | `bash scripts/package_app.sh` | release 构建成功，生成 TokenLink.app | PASS | ad-hoc 签名 |
| plist 校验 | `plutil -lint TokenLink.app/Contents/Info.plist` | OK | PASS | — |
| 签名校验 | `codesign --verify --deep --strict TokenLink.app` | 通过 | PASS | — |
| 应用启动与 UI | `open TokenLink.app` 检查菜单栏与四个路由 | 未执行（需交互会话，会触发蓝牙授权弹窗） | NOT VERIFIED | 需人工在图形会话中验证 |
| Codex 真实额度 | 本机 codex app-server 联调 | 未执行 | NOT VERIFIED | 避免在本报告中暴露账户信息 |
| Kimi / MiniMax / GLM 真实额度 | 用户自有 Key 走 UI/Keychain 验证 | 未执行 | NOT VERIFIED | 按计划要求由用户凭据触发，不写入仓库 |
| StopWatch 硬件 | BLE 发现、绑定、ATT ACK、表盘显示 | 未执行 | NOT VERIFIED | 手头无 C152 硬件记录；CoreBluetoothTransport 为编译验证，行为测试基于 fake transport |

## 说明

- 所有自动化验证在 clean build 目录下重跑通过。
- 验证过程中发现并修复：Codex fixture JSON 少一个右括号、`CodexProvider.fetch()` 的 `stop()` 竞态、CBUUID 静态常量的 Sendable 冲突、`Regex` 静态属性的并发安全检查。
